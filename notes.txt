Nossa tabela possui o campo id, porém esse campo não está sendo autoincrementado, portanto, devemos ajustar isso
Usamos a anotação a seguir em todas as variáveis que representam ids:

@GeneratedValue(strategy = GenerationType.IDENTITY)

A strategy passada para a anotação indica que o banco de dados será o responsável pela criação dos ids