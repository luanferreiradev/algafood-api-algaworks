O desafio se trata de criar os métodos para a consulta de recursos de restaurante.
Teremos o collection resource e o singleton resource, GET /restaurantes e GET /restaurantes/{id}

Obs: Removemos a dependência do Jackson Xml, pois não terá uso na nossa API. Também removemos as classes e os
métodos que utilizavam a dependência.